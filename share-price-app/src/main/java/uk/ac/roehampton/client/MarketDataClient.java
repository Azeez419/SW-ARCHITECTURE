package uk.ac.roehampton.client;

/**
 * MarketDataClient is responsible for communicating with
 * an external market data provider (e.g. Yahoo Finance API).
 *
 * It handles:
 * - making HTTP requests to fetch share price data
 * - parsing responses returned by the external service
 *
 * This class isolates all external API communication
 * so that the rest of the application does not depend
 * directly on third-party services.
 */
public class MarketDataClient {

}

